# ACC-20260724-e5_12 — 全域指標手勢（E5-12）獨立驗收

- Result: **pass**
- Verifier: **V1.4**（獨立驗收者，非實作者；實作者 I1.15）
- Date: 2026-07-24 (UTC+0) | Branch: feat/e5_12 | Unit: E5-12 | Risk: medium（能力閘控項）
- Skill: ai-sdlc-autopilot

## 驗收依據
- `docs/backlog/BUILD.md`（C++17 / CMake / CTest 建置慣例、硬性約束表 NFR-02 / NFR-03、相位 1 = Mac/null 期規範）
- `docs/structure/units/e5_12.md`（單元結構註記）、`docs/changes/CHG-20260724-e5_12.md`（變更說明與降級契約）
- 實作：`src/events/e5_12/global_gesture.hpp` / `global_gesture.cpp` / `CMakeLists.txt`
- 上游相依（已合併，可讀不可改）：`src/kernel/e1_21/capability_matrix.hpp` / `capability_matrix.cpp`
- 測試：`tests/e5/test_e5_12.cpp`

## 獨立審查結論

### 1. 介面平台中立（NFR-02）— 通過
- 手勢以**具名列舉** `GestureType`（SwipeLeft/Right/Up/Down、CornerTopLeft/TopRight/BottomLeft/BottomRight、
  PinchIn/PinchOut）表達；角落以具名 `CornerX` 而非螢幕像素座標表達。
- `Gesture` 為純資料：具名 `type` + 平台中立正規化 `magnitude` + 人類可讀 `detail`，**無絕對座標、無數字 z-order**。
- 獨立掃描（`grep -niE 'pixel|screen_x|screen_y|absolute|z_order|coordinate|\.x|\.y'`）於 hpp/cpp **無任何實體命中**。

### 2. 相位 1 只有 null 後端、無真實 OS 呼叫 — 通過
- 唯一實作為 `NullGlobalGestures`，手勢僅由 `inject()` 手動注入；無真實後端綁定。
- 獨立掃描 `#ifdef|_WIN32|win32|cocoa|objc|#import|CGEvent|NSEvent|X11|dlopen|system(` 於 hpp/cpp
  唯一命中為第 14 行**註解**（陳述禁令本身），無任何實體平台分支或 OS 呼叫。backend_guard 語意成立。

### 3. E1-21 能力矩陣為能力可用性單一真源 — 通過
- `NullGlobalGestures::from_capability(matrix, id)` 直接以 `matrix.has(id)` 決定 `has()`；不自帶任何平台判斷。
- E1-21 `has()` 對**未宣告能力保守回 false**。獨立核對 `capability_matrix.cpp` 的 `default_decls()`：
  **`input.gesture` 未被宣告**，故相位 1 預設矩陣下 `has()==false`（保守不可用）——與介面文件、
  測試 `FromCapabilityUndeclaredIsUnavailable` / `MatrixDrivenUnavailableTriggersFallback` 一致。

## 降級路徑測試覆核（能力閘控項 medium 通過的必要條件）— 通過

驗收硬性要求：`has()==false` 時「不崩、不誤觸、明確回報不可用」。逐項覆核 `GlobalGestureDegradation`
測試群（6 案），確認皆為**真實 assertion 覆蓋**，非空殼：

| 驗收要求 | 覆蓋測試 | 覆核結論 |
|---|---|---|
| **明確回報不可用** | `HasReportsUnavailableClearly`（`has()==false`）、`SubscribeRejectedWhenUnavailable`（`subscribe` 回 0、`listener_count()==0`） | 呼叫端可由 `has()` 或 `subscribe` 回 0 明確得知不可用 ✓ |
| **不誤觸** | `InjectNeverMisfiresWhenUnavailable`（即使有瑕疵呼叫端漏查 `has()` 仍訂閱，`inject` 各手勢皆 no-op、`misfires==0`）；`SubscribeRejectedWhenUnavailable`（不建立假訂閱） | 不可用時不存在任何會被觸發的回呼 ✓ |
| **不崩** | `AllOperationsSafeWhenUnavailable`（`has/subscribe/unsubscribe/listener_count/inject` 全套 `EXPECT_NO_THROW`） | 全介面操作在不可用態皆安全不拋 ✓ |
| **端到端降級範式** | `GatedCallerFallsBackCleanly`（以 `has()` 閘控主路徑/fallback）、`MatrixDrivenUnavailableTriggersFallback`（由 E1-21 矩陣端到端判定不可用→降級） | 演示了呼叫端如何安全降級並回退 ✓ |

實作面亦已覆核：`subscribe()` 於 `!available_` 時**先於**空 listener 檢查即回 0（降級契約基石，
呼叫端漏查 `has()` 也不會建立永不觸發的假訂閱）；`inject()` 於 `!available_` 時直接 return no-op。
相位 1 無真實後端，降級行為於 null 後端上驗證即為唯一且正確的可測面，契約文件並載明相位 2
真實後端須遵守同一契約。**降級路徑測試確實滿足能力閘控項的驗收要求。**

## 獨立重跑結果（不改任何碼）

於 worktree scratchpad 自寫最小 gtest 墊片（暫存、未 commit），以指定旗標獨立重編：

```
g++ -std=c++17 -Wall -Wextra \
    -I <shim> -I src/events/e5_12 -I src/kernel/e1_21 \
    src/events/e5_12/global_gesture.cpp \
    src/kernel/e1_21/capability_matrix.cpp \
    tests/e5/test_e5_12.cpp <shim>/gtest_main.cpp -o test_e5_12_local
```

- 編譯：**exit 0，零警告**（`-Wall -Wextra` 全綠）。
- 執行：**18 tests run, 71 checks, 0 failing checks, 0 failing tests**（全 PASS，exit 0）。
- 涵蓋：能力可用路徑 9、能力矩陣整合 3、降級路徑 6。

## 其他發現
- CMakeLists 對 E1-21 以 `PUBLIC` 連結，使能力矩陣 include 目錄傳遞給測試目標，與 BUILD.md 樣板一致；
  `add_test(NAME e5_12)` 已註冊（G3 真空綠燈防線滿足）。
- 無阻斷性問題。CHG 文件所述「18 案例 / 71 checks」與本次獨立重跑完全吻合。

## 判定
**pass** — E5-12 平台中立、相位 1 純 null 後端、以 E1-21 為能力單一真源；降級路徑測試確實涵蓋
「不崩、不誤觸、明確回報不可用」三項硬性要求，滿足能力閘控項 medium 之通過條件。獨立重跑全綠零警告。
