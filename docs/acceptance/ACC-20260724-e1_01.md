# ACC-20260724-e1_01 — 具名圖層列舉與 z-order 維持（E1-01）獨立操作驗收

- Date: 2026-07-29 | Unit: E1-01 | Verifier: V1.5 | Branch: feat/e1_01

Result: pass

## 驗收範圍

對 E1-01「具名圖層列舉與 z-order 維持」的已實作 commit（`b026093`）做**獨立**操作驗收。
驗收者 V1.5 與實作者 `Implemented by: I1.96`（見 `docs/changes/CHG-20260724-e1_01.md` 表頭）為不同人。
覆核對象：

- `docs/changes/CHG-20260724-e1_01.md`、`docs/structure/units/e1_01.md`、`docs/worklog/I1.96-e1_01.md`
- `src/kernel/e1_01/{layer_stack.hpp, layer_stack.cpp, CMakeLists.txt}`
- `tests/e1/test_e1_01.cpp`
- 上游（可讀不可改）：`src/kernel/e1_21/{capability_matrix.hpp, capability_matrix.cpp}`、
  `src/kernel/backend/null/{null_backend.hpp, null_backend.cpp}`（`SurfaceLayer` 具名圖層 / `SurfaceId`）

驗收方法為獨立覆核（不預設實作者宣稱為真）＋以自寫 gtest 墊片重跑全部測試＋另寫獨立不變式探針。

## Scope 覆核結果 — pass

`git diff --name-only main...HEAD` 實得 7 檔，全數落在 write_scope
（`src/kernel/e1_01/**` + `tests/e1/test_e1_01*` + COMMON docs）：

- `src/kernel/e1_01/layer_stack.hpp`（write_scope 核心）
- `src/kernel/e1_01/layer_stack.cpp`（write_scope 核心）
- `src/kernel/e1_01/CMakeLists.txt`（write_scope 核心）
- `tests/e1/test_e1_01.cpp`（write_scope 測試）
- `docs/changes/CHG-20260724-e1_01.md`（COMMON 變更紀錄）
- `docs/structure/units/e1_01.md`（COMMON 單元結構註記；未動共用 `directory.md`）
- `docs/worklog/I1.96-e1_01.md`（COMMON 施工 worklog）

未觸及 `units.json`、根 `CMakeLists.txt`、上游 E1-21 / E1-24 單元或任何鎖定範圍以外的原始碼。無越界。

## backend_guard／平台中立／NFR 覆核 — pass

- **backend_guard — pass**：`git diff --name-only main...HEAD | grep -iE 'backend/(win32|cocoa)'`
  回 NONE；本單元未新增或觸及任何 `src/kernel/backend/win32|cocoa/` 路徑。
- **無平台分支 / 無真實 OS API — pass**：對 `src/kernel/e1_01/*.{hpp,cpp}` grep
  `#ifdef|#ifndef|win32|cocoa|windows.h|CoreGraphics|objc|#import|__APPLE__|_WIN32`，
  唯一命中為 include guard（`DS_KERNEL_E1_01_LAYER_STACK_HPP`）與**註解**中「相位 1 …無 #ifdef /
  win32 / cocoa 平台分支」的自我約束說明；程式碼實體無任何平台分支或真實 OS API。實作全為記憶體
  狀態（`std::vector<Entry>` 指派表），任何平台可編譯執行。
- **NFR-02（鐵律，本單元重點）— pass**：對外 API **只有具名圖層 + 具名關係**。自行 grep
  `z[_-]?order|z[_-]?index`（不分大小寫）於 `src/kernel/e1_01/` 與 `tests/e1/test_e1_01.cpp`——所有命中
  **僅出現於註解與單元標題**（「E1-01 …z-order 維持」「不出現數字 z-order / z-index」等說明文字），
  **程式實體零命中**。另 grep 數字 z 賦值（`z\s*=\s*[0-9]|zorder|\.z\b|->z\b`）與絕對座標欄位
  （`coord|coordinate|absolute|pixel|pos_?x|pos_?y|abs_?x|abs_?y`）——**皆零命中**。
  對外詞彙全為具名：surface 以 `SurfaceId`（`std::string`）指涉、圖層以具名列舉 `SurfaceLayer`
  （Wallpaper…Topmost）表達、圖層相對順序以具名列舉 `LayerRelation{Below,Same,Above}` 表達、
  指派結果以具名 `LayerAssign{Ok,Moved,RejectedEmptyId,RejectedNoCapability}` 表達。
  內部語意序 `semantic_index()` 置於 `.cpp` 的**匿名 namespace**（比 private 成員更封閉），不出現在
  標頭、不對外當數字層級用；`stacking_order()` 每次由「具名圖層清單 × 加入序」重新導出，不以整數
  欄位儲存層級。NFR-02 成立。
- **NFR-03（能力閘控）— pass**：改動狀態的 `assign` / `remove` 皆先 `has(layer_capability())`
  （`"kernel.surface"`）閘控。覆核 `assign`：空 id → `RejectedEmptyId`；`has()==false` →
  `RejectedNoCapability` 且**不 push_back、狀態不變**；既有 id → `Moved`（保留加入序位置）；否則
  `Ok`。覆核 `remove`：`has()==false` 時**直接回 false 不改狀態**，未知 id 回 false 不崩潰。
  能力來源為上游 E1-21 `CapabilityMatrix`（`kernel.surface` 於 `defaults()` 宣告 default_available=true）；
  未知能力沿用 E1-21 語意保守回 false（`stack.has("does.not.exist")==false`，已驗）。

## 重跑測試 — 實際命令與結果 — pass

本機無 cmake。以自寫 scratchpad gtest 墊片（單一 header `gtest/gtest.h`，巨集**單次求值**、
自帶 `main()` runner，**不進 repo**）連上游 e1_21 + null 後端 + e1_01 + 測試，單一命令、全絕對
路徑、無 cd / 無重導：

```
g++ -std=c++17 -Wall -Wextra \
  -I <repo>/src/kernel/e1_01 \
  -I <repo>/src/kernel/e1_21 \
  -I <repo>/src/kernel/backend/null \
  -I <scratchpad>/e1_01_acc \
  <repo>/src/kernel/e1_21/capability_matrix.cpp \
  <repo>/src/kernel/backend/null/null_backend.cpp \
  <repo>/src/kernel/e1_01/layer_stack.cpp \
  <repo>/tests/e1/test_e1_01.cpp \
  -o <scratchpad>/e1_01_acc/test_e1_01_local \
  && <scratchpad>/e1_01_acc/test_e1_01_local
```

結果：

- 編譯：成功，**0 警告 / 0 錯誤**（`-Wall -Wextra`）。另以相同命令加 `-Werror` 重編仍成功通過，
  確證零警告。
- 執行：**17 tests: 17 passed, 0 failed**。
- 涵蓋案例：LayersBottomToTopAreOrderedAndComplete、LayerNamesAreStableAndNamed、
  CompareLayersBySemantics、AssignNewSurfaceOk、EmptyIdRejected、ReassignMovesLayerKeepingIdentity、
  CrossLayerOrderFollowsLayerSemantics、LayerSemanticsBeatInsertionOrder、
  SameLayerPreservesInsertionOrder、InterleavedAssignmentsGroupByLayerThenInsertion、
  RemoveDropsAssignment、UnassignedQueriesAreConservative、EmptyStackTopAndBottomAreEmpty、
  DefaultMatrixEnablesLayerCapability、AssignRefusedWhenCapabilityUnavailable、
  RemoveRefusedWhenCapabilityUnavailable、UnknownCapabilityIsConservativelyFalse。

## 圖層語意不變式實測 — pass

除實作者測試（`StackingOrder.LayerSemanticsBeatInsertionOrder`）外，另寫**獨立探針** `probe.cpp`
（不進 repo）直接以本單元 API 驗證核心不變式，不預設實作者測試為真：

- **先加 Topmost、後加 Wallpaper** → `stacking_order()`（由底到頂）實得 `[wp] [top]`：桌布仍在底、
  最上層仍在頂；`bottommost()=="wp"`、`topmost()=="top"`、`is_below("wp","top")==true`、
  `is_above("top","wp")==true`。**圖層語意優先於加入序**成立。
- **NFR-03 拒絕路徑**：以 `kernel.surface` 設為不可用之矩陣建構，`assign` 回 `RejectedNoCapability`
  且 `size()==0`、`contains("x")==false`——狀態未變。
- **語意分層重組**：交錯以 Overlay / Normal / Wallpaper 指派，`stacking_order()` 實得 `{wp, n, o}`，
  與加入序無關、僅依圖層語意。

探針編譯零警告、執行 `PROBE PASS (0 checks failed)`。

## 與 CHG 宣稱的比對

- CHG 宣稱「gtest 17 案例、全過、零警告」——本次獨立重跑 **17 tests 全過、零警告**，一致。
- CHG 宣稱「對外 API 只有具名圖層與具名關係、內部 `semantic_index` 為 private 不對外當數字層級、
  堆疊順序不以整數欄位儲存而每次由具名圖層清單導出」——覆核**一致**（semantic_index 實置於 .cpp
  匿名 namespace，更嚴於 private）。
- CHG 宣稱「無 #ifdef / win32 / cocoa / 真實 OS API」——覆核**一致**。
- CHG 宣稱「NFR-03 閘控於 assign / remove、未知能力保守回 false」——覆核**一致**。
- CHG「回溯」宣稱「純新增單元，未改上游 / 根 CMakeLists / units.json / directory.md」——與實得
  7 檔 diff **一致**。
- `Implemented by: I1.96` 與本驗收 `Verifier: V1.5` 為不同人——**成立**。

## 判定理由

Scope 未越界（7 檔全落 write_scope）；backend_guard 與相位 1 平台中立約束成立（無 win32 / cocoa、
無 #ifdef、無真實 OS API）；**NFR-02 鐵律經自行 grep 確認對外零數字 z-order / z-index / 絕對座標，
命中僅止於註解**；NFR-03 閘控於 assign / remove 且拒絕不改狀態；核心不變式「圖層語意優先於加入序」
經實作者測試與獨立探針雙重實測成立；獨立重跑 17 tests 全數通過且零警告（`-Werror` 亦通過），與 CHG
宣稱一致。判定 pass。

Verifier: V1.5
