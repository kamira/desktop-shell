# ACC-20260724-e1_24 — null 後端參考實作（E1-24）獨立操作驗收

- Date: 2026-07-29 | Unit: E1-24 | Verifier: V1.2 | Branch: feat/e1_24

Result: pass

## 驗收範圍

對 E1-24「null 後端參考實作」的已實作 commit 做**獨立**操作驗收（驗收者 V1.2 與實作者
`Implemented by: I1.94` 為不同人，見 `docs/changes/CHG-20260724-e1_24.md` 表頭）。覆核對象：

- `docs/changes/CHG-20260724-e1_24.md`、`docs/structure/units/e1_24.md`
- `src/kernel/backend/null/{null_backend.hpp, null_backend.cpp, CMakeLists.txt}`
- `tests/e1/test_backend_null.cpp`
- 上游（可讀不可改）：`src/kernel/e1_21/{capability_matrix.hpp, capability_matrix.cpp}`

驗收方法為獨立覆核（不預設實作者宣稱為真）＋以自寫 gtest 墊片重跑全部測試。

## Scope 覆核結果 — pass

`git diff --name-only origin/main...feat/e1_24` 實得 7 檔，全數落在 write_scope
（`src/kernel/backend/null/**` + `tests/e1/test_backend_null*` + COMMON docs）：

- `src/kernel/backend/null/null_backend.hpp`（write_scope 核心）
- `src/kernel/backend/null/null_backend.cpp`（write_scope 核心）
- `src/kernel/backend/null/CMakeLists.txt`（write_scope 核心）
- `tests/e1/test_backend_null.cpp`（write_scope 測試）
- `docs/changes/CHG-20260724-e1_24.md`（COMMON 變更紀錄）
- `docs/structure/units/e1_24.md`（COMMON 單元結構註記；未動共用 `directory.md`）
- `docs/worklog/I1.94-e1_24.md`（COMMON 施工 worklog）

未觸及 `units.json`、根 `CMakeLists.txt`、上游 E1-21 單元或任何鎖定範圍以外的原始碼。無越界。

## backend_guard（G1b）／平台中立／NFR 覆核 — pass

- **backend_guard G1b — pass**：`src/kernel/backend/` 下**只有** `null/` 一個子目錄；
  `find src -iname '*win32*' -o -iname '*cocoa*'`（排除 null）回 NONE，全 src 無 win32 / cocoa
  目錄或檔案。相位 1（Mac / 平台中立期）約束成立。
- **無平台分支 / 無真實 OS API — pass**：對 `null_backend.{hpp,cpp}` grep
  `#ifdef|#ifndef|#if |_WIN32|__APPLE__|NSWindow|CreateWindowEx|#import|#include <windows|Cocoa/|Foundation/`，
  唯一命中為 include guard（`DS_KERNEL_BACKEND_NULL_NULL_BACKEND_HPP`）與**註解**中「不得出現…」
  的自我約束說明；程式碼實體無任何平台分支或真實 OS 視窗 API。cpp 全為 no-op / 記憶體狀態
  （`std::vector` surface 表 + 呼叫計數），任何平台可編譯執行。
- **NFR-02（具名指涉、無絕對座標 / 數字 z-order / 數字 handle）— pass**：surface 一律以具名
  `SurfaceId`（`std::string`）指涉；圖層以具名列舉 `SurfaceLayer`（Wallpaper…Topmost）表達，
  無數字層級；`SurfaceProfile` 為四具名列舉（layer / input / hit / lifecycle），純資料無座標；
  `InputEvent` 不含像素座標。grep `set_position|x, y|z_order|handle|HWND` 對程式碼實體零命中
  （僅註解提及禁止項）。內部雖以 `std::vector` 順序保存，但順序**不對外以數字 index 暴露**，
  對外 API 全走具名鍵。
- **NFR-03（能力查詢經 E1-21 has()）— pass**：`NullKernelBackend` 持有 E1-21 `CapabilityMatrix`
  為單一資料來源；`has(id)` 委派 `caps_.has(id)`；未宣告 / 未知能力保守回 false。預設即
  `CapabilityMatrix::defaults()`（基礎能力如 render.paint / kernel.surface 可用；可選能力如
  host.tray_icon / actuator.brightness 一律不可用）；建構子可注入自訂矩陣。
- **介面完整性 / null 語意 — pass**：`KernelBackend` 純虛擬抽象契約（name / 生命週期 /
  能力 / K1 surface / K2 paint / K3 input）；`NullKernelBackend final` 覆寫全部方法為 no-op /
  記憶體語意：init 冪等回 true、shutdown 清空 surface 並冪等、K1 空 / 重複 / 未知 id 結構化拒絕、
  K2 begin 不可重入且 end 需先 begin、K3 poll_input 永遠回空並計數。邊界情形皆保守回 false /
  nullptr，不崩潰。

## 重跑測試 — 實際命令與結果 — pass

本機無 cmake。以自寫 scratchpad gtest 墊片（單一 header `gtest/gtest.h` + `main.cpp` runner，
巨集單次求值，**不進 repo**）連上游 e1_21 + null 後端 + 測試，單一命令、全絕對路徑、無 cd / 無重導、
`-Werror` 使任何警告即編譯失敗：

```
g++ -std=c++17 -Wall -Wextra -Werror \
  -I <repo>/src/kernel/e1_21 \
  -I <repo>/src/kernel/backend/null \
  -I <scratchpad>/e1_24_v12 \
  <repo>/src/kernel/e1_21/capability_matrix.cpp \
  <repo>/src/kernel/backend/null/null_backend.cpp \
  <repo>/tests/e1/test_backend_null.cpp \
  <scratchpad>/e1_24_v12/main.cpp \
  -o <scratchpad>/e1_24_v12/test_backend_null_v12 \
  && <scratchpad>/e1_24_v12/test_backend_null_v12
```

結果：
- 編譯：成功，**0 警告 / 0 錯誤**（`-Wall -Wextra -Werror` 下通過）。
- 執行：**14 tests run, 14 passed, 0 failed**。
- 涵蓋案例：NameIsNull、InitLifecycle、ShutdownIsIdempotentAndReleasesSurfaces、
  CapabilitiesReportedViaE1_21、CustomCapabilityMatrixInjectable、CreateSurfaceAndDuplicateRejection、
  SurfaceProfileStoredFaithfully、ShowHideSurface、DestroySurface、PaintFrameLifecycle、
  SetInputPolicy、PollInputAlwaysEmpty、UsableThroughBasePointer、DefaultProfileIsSaneNeutral。

## 與 CHG 宣稱的比對

- CHG `Status` 宣稱「14 tests run, 14 passed, 0 failed，零警告」——本次獨立重跑**一致**。
- CHG 宣稱「無平台分支、無真實 OS API、無 win32 / cocoa 目錄、符合 backend_guard G1b」——覆核**一致**。
- CHG 宣稱「NFR-02 具名指涉、NFR-03 能力查詢經 E1-21」——覆核**一致**。
- CHG「不含」清單（不實作真實後端、不寫 E1-25 契約測試組、未動 units.json / 根 CMakeLists /
  directory.md）——與實得 diff **一致**。
- `Implemented by: I1.94` 與本驗收 `Verifier: V1.2` 為不同人——**成立**。

## 判定理由

Scope 未越界；backend_guard G1b 與相位 1 平台中立約束成立（無 win32 / cocoa、無 #ifdef、無真實
OS API）；NFR-02 / NFR-03 皆滿足；`NullKernelBackend` 完整實作抽象介面為 no-op / 記憶體狀態，
任何平台可編譯執行；獨立重跑 14 tests 全數通過且零警告，與 CHG 宣稱一致。判定 **pass**。
