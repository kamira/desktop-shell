# CHG-20260723-16 — A1 語言決策定案（不限語言 / C·C++ 允許）、驗證器歸類為 example

- Date: 2026-07-24 (UTC+0) | Branch: chore/a1-language | Risk: low | Implemented by: I1
- Skill: ai-sdlc-autopilot v1.1

## Motivation
A1 原為待決假設「引擎語言為 Qt/QML，待 wave 0 驗證」。使用者定案：
**語言不限定任何選擇，唯一要求是能在各目標平台獲取系統資訊並操作系統（原生存取能力），
C/C++ 明確允許。** 本 CHG 把此決策落成文件；並依指示把四個階段驗證器歸類為 `example`。

## Impact（僅文件；未動 `units.json`）

### 1. A1 語言決策定案
| 檔案 | 變更 |
|---|---|
| `docs/ai-guideline.md` §3 | 新增決策列 **Q4 實作語言**：不限定；擇能跨平台獲取/操作系統者（C/C++ 允許）；跨平台性由 API 面約束保證，不由語言保證 |
| `docs/ai-guideline.md` §8 | **A1 由待決轉為已定案**，指向 Q4，「原 Qt/QML 假設解除」 |
| `docs/requirements.md` | 「三個定案決策」→「四個」，新增「實作語言」一節；分相位段的 `QML` 舉例語言中立化 |
| `docs/architecture.md` | A1 驗證範圍敘述改為語言中立（驗的是 `src/` 30 項的 API 面能否跨平台表達，非語言） |

**核心論點**：跨平台性不靠語言保證，靠 API 面約束保證（NFR-02 不得絕對座標、
NFR-03 `has()` 閘控、`backend_guard` 契約測試不得含平台分支）。語言可自由選，被機器盯住的是 API 面。

### 2. 驗證器歸類為 `example`
`docs/architecture.md` 新增「驗證器歸類為 example」一節：四個階段驗證器
（A：CPU/GPU Usage Widget；B：最小角色範例；C：最小啟動器範例；D：最小浮層範例）
與 `E8-06` 的 `hello-module`，**分類為 `example`（非 `artifact`）**，位於 `examples/`。
- `artifact`（C1–C4）= 隨附的第一方內容
- `example` = 純「證明擴充點可用」的最小驗收範例；名稱本身防止它被當成交付目標

## 明確未做（等使用者給規格）
- **未把 4 個驗證器 + `E8-06` 寫進 `units.json`** —— 相依 / wave / 風險分級待規格。
  屆時 `layer` 值域會新增 `example`，總數 176 → 181。
- `E8-04` 升 P0 一併待該批 CHG。
- backlog 目前仍為 **176 單元**，派工依據不變。

## Status
Accepted — self-verified (low risk)。

## Verification
- `grep Qt/QML` 於三份文件：僅剩「假設解除」的敘述，無任何把 Qt/QML 當鎖定語言之處
- `plan.py status` 176 單元不變；`stage_check` 無違規；`backend_guard` 相位 1
  （本 CHG 未動 `units.json` 與任何腳本）
