# CHG-20260730-05 — 桌面 runtime 路線圖 + 交接文件更新（純規劃文件）

- Date: 2026-07-30 (UTC+0) | Branch: chore/phase2-handoff-docs | Risk: low | Implemented by: I1
- Skill: ai-sdlc-autopilot v1.1

## Motivation
相位 1 全部收官（176 單元 + CHG-20260730-01~04）。使用者詢問「把擴充點變成 Rainmeter 式桌面 runtime
（托盤選單 / 最上層切換 / 浮動固定 / 點擊穿透 / widget skin 設定檔）是否需要新 repo」，並要求**留紀錄 + 建立交接文件**。
本 CHG 純新增/更新規劃文件，不動任何程式碼。

## Impact（僅 docs/backlog/，無程式碼變更）
- **新增 `docs/backlog/phase2-desktop-runtime.md`**：桌面 runtime 路線圖草案。內容：
  - 需求 → 現有單元對照（E11-01 托盤 / E1-01 最上層 / E1-08 浮動固定 / E1-02 穿透 / E9+E7 設定檔…全是現成單元）。
  - 核心結論：引擎不需新 repo；缺相位 N cocoa 後端 + host shell（同 repo）；只有 skin 內容包適合獨立 repo。
  - **決策 A/B**：cocoa 是否提前（原 PHASE-PLAN 排相位 3、Windows 優先）——屬需人工核准的方向調整。
  - 完整 backlog：WS1 相位閘門翻轉（含 CI 平台 runner 卡點）、WS2 平台後端單元、WS3 host shell app、WS4 skin 套件格式。
- **更新 `docs/backlog/HANDOFF.md`**：新增「§0 最新狀態（2026-07-30）」段，涵蓋 176 收官 + CHG-01~04 + 本機 cmake 環境
  + 指向桌面 runtime 路線圖，並標示 §1~2 為舊快照。

## 明確不改
- 未動任何 `src/` / `engine/` / `modules/` / `content/` / `apps/` / `examples/` 程式碼。
- 未動 `units.json` / `phase.json`（路線圖的暫名單元**尚未**正式編號、加入 units.json——待 A/B 決策後再治理化）。
- 未實作任何後端 / host shell（純規劃）。

## Status
Accepted — self-verified（low risk，純規劃文件）。

## Verification
- 純 Markdown 新增/更新，無程式碼、無建置影響（G3 全建與合併前一致）。
- 分支 `chore/phase2-handoff-docs` 非 feat/ → 經 CHG-20260730-01 的 infra 閘門走 repo 級閘門後自動合併。
