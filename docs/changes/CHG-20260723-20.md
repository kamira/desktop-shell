# CHG-20260723-20 — G4 同步改為每單元獨立結構檔，消除並行單元的 directory.md 衝突

- Date: 2026-07-24 (UTC+0) | Branch: chore/per-unit-structure | Risk: low | Implemented by: I1
- Skill: ai-sdlc-autopilot v1.1

## Motivation
wave 0 首批並行派工暴露一個系統性問題：G4 要求「動 src|engine|modules/ 就得動 docs/structure/」，
先前慣例是各單元**編輯共用的 `docs/structure/directory.md`**。並行單元同時改同一檔案 →
- 合併衝突（E5-08 #10 卡 CONFLICTING，需人工解）
- 更糟：後合併的 squash-merge **覆蓋**先合併單元的結構註記（E11-03/E5-04/E1-17 的註記在 main 上遺失）

## Impact
`docs/backlog/BUILD.md`：G4 同步方式改為 **每單元寫一個獨立檔 `docs/structure/units/<slug>.md`**，
**明令不得編輯共用的 `directory.md`**。

- 每單元一檔 → 永不衝突、不互相覆蓋
- 一樣滿足 G4（diff 含 `^docs/structure/`）
- `docs/structure/units/**` 已在 `scope_check` COMMON 內，可寫

## 不含
- 未追補已遺失的 E11-03/E5-04/E1-17 結構註記（其程式碼已在 main，僅結構備註缺；非阻斷，
  日後可補或由 units 檔重建）
- 未動 `units.json`、未動 G4 workflow 邏輯（G4 grep `^docs/structure/` 本就涵蓋 `units/` 子目錄）

## Status
Accepted — self-verified (low risk)。

## Verification
- G4 判定式 `grep -q '^docs/structure/'` → `docs/structure/units/<slug>.md` 命中 ✓
- `scope_check` COMMON 含 `docs/structure/**` → 單元可寫該路徑 ✓
- 後續派工簡報同步改為指示「寫 docs/structure/units/<slug>.md，不要動 directory.md」
