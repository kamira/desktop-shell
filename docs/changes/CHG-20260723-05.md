# CHG-20260723-05 — 收錄平台架構文件、解除 G1/G4 死鎖、units.json 補 layer/stage

- Date: 2026-07-24 (UTC+0) | Branch: claude/ai-sdlc-handshake-validation-380ada | Risk: low | Implemented by: I1
- Skill: ai-sdlc-autopilot v1.1

## Motivation
需求與架構改版為「平台定位」：核心提供擴充點、組件是驗證器、分四階段各配一驗證器。
三份草案（`ai-guideline` / `architecture` / `requirements`）需收錄進 repo。
同時稽核發現一個**閘門死鎖**：150/176 單元寫 `src/`，但無任何單元的 `write_scope`
含 `docs/structure/`，`scope_check` 的 COMMON 也不放行 —— 只寫 `src/` 被 G4 擋、
補寫 `docs/structure/` 被 G1 擋，兩邊都紅，**無解路徑**。

## Impact

### 1. 解除 G1/G4 死鎖（本次最關鍵）
- `scripts/scope_check.py`：`COMMON` 加入 `docs/structure/**`
- 新增 `docs/structure/directory.md` —— G4 的比對對象，先前 `docs/structure/` 根本不存在

> **這是一次治理「放寬」**（停點契約為 tighten-only，放寬需人工點頭）。
> 已獲明確指示「解」。取捨：放寬後任何單元都可寫結構文件，換取 150 個單元
> 從「不可能合併」變成可合併。替代方案（每個單元 `write_scope` 各加
> `docs/structure/<module>/**`）需改 176 筆，留待日後需要更嚴時再收緊。

### 2. 收錄架構與需求文件
| 檔案 | 動作 |
|---|---|
| `docs/requirements.md` | 新增（v2.0 敘述性需求） |
| `docs/architecture.md` | 新增（平台定位、五擴充點、四階段） |
| `docs/ai-guideline.md` | 改版 v1.1：加 §0 敘述性需求指標、NFR-07（`E8-06` 自證）、NFR-08（`stage_check.py`）、平台定位、待決 A4–A6 |
| `docs/structure/directory.md` | 新增（目錄結構，四結構之一） |

### 3. `units.json` 補 `layer` / `stage`（純機械推導，未新增或修改任何單元）
- `layer`：`write_scope` 含 `content/` 或 `apps/` → `component`，其餘 → `core`
- `stage`：`group` 對映 —— 核心+監控 → A、角色 → B、啟動 → C、工具 → D
- `totals` 新增 `by_layer` = {core 152, component 24}、`by_stage` = {A 116, B 24, C 13, D 23}
- 既有欄位一律未動，`plan.py` / `scope_check.py` 不受影響

## 數字處理原則
草案的 181 / core 160 / component 21 **無法由 `units.json` 複算**，且新增的 5 個單元
只點名了 `E8-06`，其餘 4 個無出處。因此三份文件一律採「**現況（可複算）+ 目標**」
雙欄呈現，並明載「派工一律以 `units.json` 現況為準」。
現況：176 單元、core 152 / component 24、medium 16 / low 160、wave0 18、
階段 A116/B24/C13/D23。

已修正草案中的殘留數字：`ai-guideline` §4 的 173、`architecture` 的
「平台核心 97 項」「56 個模組」「其餘 92 項」「176→177」。

### 經查證為真、且比草案更強的論點
- 29 項語意分歧**全部落在 core**，component 為 **0**
- 24 個組件**全部是平台中立**（連 API 分歧都沒有）
→ 故「契約定稿後組件可無限並行且與平台後端無關」成立。

## 未納入本 CHG（待決）
| # | 項目 | 卡在哪 |
|---|---|---|
| A4 | 目標 181 中未點名的 4 個單元 | 需清單或產生器 |
| A5 | 組件遷至 `modules/` + `examples/` | 需改 24 筆 `write_scope`，屬設計決策 |
| A6 | `E8-06` 入 units.json、`E8-04` 升 P0 | 相依/wave/風險分級屬設計判斷，不由 I1 逕自決定 |
| — | `stage_check.py`（NFR-08） | 腳本未寫；且現有資料已有 1 筆違規：`E11-02`(階段A) 依賴 `E7-13`(階段C) |
| — | NFR-02/03 建置期 lint | CI 無 C++/Qt 建置步驟，兩條目前是空的 |

## Status
Accepted — self-verified (low risk)。

## Verification
- **死鎖解除（端到端）**：模擬 E1-21 同時寫 `src/kernel/e1_21/` 與 `docs/structure/`，
  `scope_check --unit E1-21` → `OK — 2 個檔案全部落在 E1-21 範圍內`（G1 PASS）；
  同一份 diff 過 G4 邏輯 → PASS。修補前此組合必紅。
- `python3 -c "import json;json.load(...)"` → `units.json` 仍為合法 JSON
- `python3 scripts/plan.py status` → 176 單元、wave 分佈不變
- `python3 scripts/plan.py unit-for-branch feat/e2_01` → id=E2-01 / risk=medium / phase=1
- `python3 scripts/backend_guard.py` → 相位 1 通過
- `units.json` diff 為 363 新增 / 1 刪除，該刪除僅為 `"backend_followup": 63` 補逗號
