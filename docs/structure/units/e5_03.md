# 結構註記 — E5-03 生命週期事件

- 單元：E5-03（生命週期事件，engine 層 / 事件 / low）
- 目錄位置：`engine/events/e5_03/`（平台中立純邏輯，非全域事件；換平台一行不動）
- 放了什麼：
  - `lifecycle_source.hpp` / `.cpp`：`LifecyclePhase` enum（Created/Shown/Hidden/
    Activated/Deactivated/Destroyed）、`LifecycleEvent{ surface, from, to }`、
    `LifecycleSource`（create / transition / subscribe / unsubscribe / phase_of），
    以及自由函式 `is_legal_transition()`（固定狀態機表）。
  - `CMakeLists.txt`：`add_library(e5_03)` + `test_e5_03` + `add_test(NAME e5_03)`。
- 對外介面一句話：surface / profile 生命週期相位轉換的發佈與訂閱，合法轉換以狀態機
  約束（Destroyed 為終端，不可再轉出），轉換由呼叫端 / 相位 2 真實後端驅動。
- 測試：`tests/e5/test_e5_03.cpp`（15 個 gtest）。
