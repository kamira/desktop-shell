# 結構註記 — E9-03 可互換元件組合

- **目錄位置**：`engine/package/e9_03/`（layer=engine，stage=A）。
- **放了什麼**：
  - `composition.hpp` / `composition.cpp` — 組合層 API：`ComponentSlot`（具名插槽/角色 + `required_kinds` 介面）、`CompatibilityResult`、`BindResult`、`check_compatibility()`、以及 `Composition` 類別（`add_slot`/`bind`/`swap`/`unbind`/`is_bound`/`bound_component`/`compatible_candidates`/`validate`）。
  - `CMakeLists.txt` — `add_library(e9_03)`，`target_link_libraries(e9_03 PUBLIC e9_01)`；註冊 `test_e9_03`。
- **對外介面（一句話）**：以 E9-01 的元件（`Package`）為單位，讓元件能對「插槽/角色」以可互換方式綁定/替換，並在組合層驗證相容性、列舉相容候選；不相容一律明確報錯、不靜默。
- **相依**：上游 E9-01（`engine/package/e9_01/`，經其再傳遞 E9-02 manifest）。命名空間 `ds::package`，與 E9-01/E9-02 一致。
- **平台中立**：純邏輯，無任何 `#ifdef` / 平台分支。
- **測試**：`tests/e9/test_e9_03.cpp`（gtest，19 個案例）。
