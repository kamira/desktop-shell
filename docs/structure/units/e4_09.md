# 結構註記 — E4-09 動畫驅動源

- 目錄位置：`engine/render/e4_09/`（engine 層 / render 繪製基座 / 平台無關純邏輯）。
- 內容：`animation_driver.hpp|cpp`（`AnimationDriver`：綁一個 `ds::events::HeartbeatSource`，
  把心跳脈衝轉為對各註冊動畫的時間推進；`add`/`remove`/`pause`/`resume`/`is_paused` 與整體
  `pause_all`/`resume_all`；幀資料 `AnimationFrame`{ id, dt, elapsed, frame }、回呼 `AnimationCallback`；
  兩階段派發保證回呼中變更動畫集安全；解構自動解訂閱）、
  `CMakeLists.txt`（`add_library(e4_09)` + `target_link_libraries(e4_09 PUBLIC e5_04)` + `test_e4_09`，
  根層自動 glob 收入）。
- 相依：上游 E5-04 心跳事件（`engine/events/e5_04/`，可讀不可改；提供邏輯時間脈衝）。
- 對外介面：命名空間 `ds::render`；由所綁心跳 `advance(dt)` 注入邏輯時間，每脈衝餵 dt 給未暫停動畫，
  供 tween / 幀動畫消費；不綁真實時鐘 / 計時器 / 繪製後端。
- 測試：`tests/e4/test_e4_09.cpp`（12 個 gtest 案例，`add_test(NAME e4_09)`）。
