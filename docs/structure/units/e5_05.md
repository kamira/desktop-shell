# 結構註記 — E5-05 全域熱鍵註冊

- 單元：E5-05（platform 層 / 事件 / Risk: low）
- 目錄位置：`src/events/e5_05/`（全域熱鍵需 OS 後端，依 directory.md 置於 `src/events/`）
- 測試：`tests/e5/test_e5_05.cpp`（CTest 名 `e5_05`）

## 放了什麼
- `global_hotkey.hpp` / `global_hotkey.cpp`：平台中立介面 + null 後端。
- `CMakeLists.txt`：`add_library(e5_05)` + `test_e5_05` + `add_test(NAME e5_05)`。

## 對外介面（一句話）
`ds::events::GlobalHotkeys`：`has()`（NFR-03 能力閘控）/ `register_hotkey(Hotkey, cb)→HotkeyId` /
`unregister(id)`；熱鍵以具名 `Modifier`+`Key` 描述（非平台掃描碼）；相位 1 由 `NullGlobalHotkeys`
以 `inject(hotkey)` 手動注入模擬按下，不綁真實 OS（無 `#ifdef` / win32 / cocoa）。
