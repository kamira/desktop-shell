# 結構註記 — H1-01 host shell app（GDI 繪製橋接 + 主迴圈 + 浮動 CPU widget）

- 層級：artifact（桌面上顯示的產出物）／新頂層目錄 `host/`
- 目錄位置：`host/`（相位 2 起的 host shell；根 CMakeLists 新增 `host/*/CMakeLists.txt` GLOB）
  - `host/paint/widget_painter.hpp` / `.cpp` — render_model → GDI 繪製橋接
  - `host/paint/CMakeLists.txt` — `add_library(h1_01_paint STATIC ...)` + `test_h1_01`
    + `add_test(NAME h1_01_paint)`，整份 `if(WIN32)`
  - `host/app/host_main.cpp` — `wWinMain` 主迴圈：建 surface → 組裝 widget → 餵真實指標 → 繪製
  - `host/app/CMakeLists.txt` — `add_executable(desktop_shell_host WIN32 ...)`，整份 `if(WIN32)`
- 測試：`tests/h1/test_h1_01.cpp`（gtest，10 案例；純函式幾何 + 記憶體 DIB 讀像素）
- 上游相依（可讀不可改）：W1-01（win32 後端）、C2-02（SystemStatusWidget / RenderModel）、
  E2-01（MetricRegistry / InMemoryMetric）、E4-03（fill_ratio）、E4-01、E7-01。

## 為什麼繪製橋接在 `host/` 而不在 win32 後端裡

兩個獨立理由，任一都足以決定：

1. **分層**：它吃 E4-03 的 `ds::elements::RenderModel`（engine / module 層型別）。
   放進 `src/kernel/backend/win32/`（platform 層）會造成 **platform 反向依賴 engine**。
2. **NFR-02**：繪製必然涉及座標，而 kernel 契約硬性禁止絕對座標——
   所以繪製**不可能**成為 kernel 契約的一部分，只能活在契約之外。

後端因此只負責「視窗、圖層、輸入」，並額外提供 win32 專屬的 `hwnd_for()` 讓 host 取得原生視窗。

## 可測試性設計

版面計算（`row_layout` / `fill_rect`）刻意抽成**不碰 GDI 的純函式**，可直接斷言幾何；
繪製本體以**記憶體 DIB**（`CreateDIBSection`）測試，能真的讀像素——
因此測試斷言的是「量表長度真的跟著 fill_ratio 走」，而不只是「函式沒有崩潰」。

## 與 `examples/cpu_gpu_validator` 的關係

兩者組裝 widget 與指標的路徑**完全相同**（同一組擴充點、同一份 render_model），
差別只在最外層呈現：驗證器畫成主控台 ASCII，本單元畫成桌面視窗像素。
這正是重點——換掉呈現層時，擴充點一個都沒改。

## 目前沒有的（後續單元）

托盤圖示與選單（E11-01/E11-02 的 win32 後端）、拖曳移動（E1-08）、
點擊穿透的 UI 切換（後端已支援 `set_input_policy`，缺選單入口）、設定持久化（E7-12）、
skin 套件格式（E9）。**因此目前沒有正常的關閉入口**：以 `--seconds N` 自動結束，
或由工作管理員結束行程。這是「最小可見成果」範圍的刻意取捨（CHG-20260803-03）。
