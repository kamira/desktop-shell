# 結構註記 — E4-07 幀序列動畫

- 目錄位置：`modules/elements/e4_07/`（module 層 / elements 子系統 / 平台無關純邏輯）。
- 內容：`frame_animation_element.hpp|cpp`（`FrameAnimationElement`：持有一組幀（值語意複製自
  注入 `ImageSource`）與一個以幀計數的「累計播放進度」；`set_frames`/`set_fps`/`fps()`
  設定；`set_loop`/`play`/`pause`/`is_playing` 播放控制；`advance(dt)` 以注入式 tick 累計
  進度（循環模式取模繞回、單次模式夾在最後一幀並標記完成）；`attach(AnimationDriver&)` 綁
  E4-09 動畫驅動源自動推進；`current_frame()`/`is_finished()`/`reset()` 查詢與控制；
  `set_scale_mode`/`set_opacity`/`set_target` 透傳給內部 E4-02 `ImageElement`；
  `render_model()` 委派其 `render_model()`，回傳相對佈局 `ImageRenderModel`）、
  `CMakeLists.txt`（`add_library(e4_07)` + `target_link_libraries(e4_07 PUBLIC e4_02 e4_09)`
  + `test_e4_07`，根層自動 glob 收入）。
- 相依：上游 E4-02 圖片元件（`modules/elements/e4_02/`，可讀不可改；提供圖片顯示與渲染描述
  模型）、上游 E4-09 動畫驅動源（`engine/render/e4_09/`，可讀不可改；提供時間推進來源）。
- 對外介面：命名空間 `ds::elements`；由呼叫端注入邏輯時間（手動 `advance` 或 `attach` 到
  E4-09 `AnimationDriver`）推進「目前應顯示第幾幀」，以 `render_model()` 取得可繪製的相對
  佈局渲染描述（NFR-02：無絕對座標 / 數字 z-order）；不綁真實時鐘 / 計時器 / 繪製後端。
- 測試：`tests/e4/test_e4_07.cpp`（16 個 gtest 案例，`add_test(NAME e4_07)`）。
