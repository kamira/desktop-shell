# 結構註記 — C1-07 全螢幕調光層 profile

- 單元：C1-07（artifact 層 / 應用 profile / Risk: low）
- 目錄位置：`content/profiles/c1_07/`
- 測試：`tests/c1/test_c1_07.cpp`（CTest 名 `c1_07`，14 個 gtest 案例）

## 放了什麼
- `dim_overlay_profile.hpp` / `.cpp`：**組裝型**單元（非新引擎邏輯）——把已合併的
  E4-30 全螢幕調光覆蓋（`DimOverlayElement`）包成單一應用 profile，收斂三種常見情境：
  - `DimProfileKind::Focus`（專注模式）、`NightShift`（夜間調光）、`PopupBackdrop`
    （彈窗背景變暗），各配一組 `DimPreset{ intensity, color }`（`default_preset()`）。
  - E4-30：調光強度 / 顏色 / 淡入淡出 / 挖洞（全數透傳）。
  - E1-01（經 E4-30 內部整合）：置於**具名頂層** `SurfaceLayer::Topmost`（`layer()` 透傳）。
- `CMakeLists.txt`：`add_library(c1_07)` + `target_link_libraries(c1_07 PUBLIC e4_30 e1_01)`
  + `test_c1_07`（`tests/c1/test_c1_07.cpp`）+ `add_test(NAME c1_07)`。

## 對外介面（一句話）
`ds::profiles::DimOverlayProfile`：`activate(intensity)` / `activate()`（套用情境預設
強度）淡入啟用 / `deactivate()` 淡出停用 / `fade(target)` 任意淡變（不套用情境顏色）/
`add_cutout` `remove_cutout` `has_cutout`（透傳 E4-30 挖洞）/ `state()` `is_active()`
`intensity()` `color()` `layer()` `alpha_supported()` `render_model()` 查詢。相位 1：純
組裝，無真實 GUI、無平台分支（無 `#ifdef` / win32 / cocoa）、無絕對座標 / 數字 z-order
（NFR-02）；非有限參數 / 能力不可用皆委派 E4-30 既有語意，報錯不靜默。
