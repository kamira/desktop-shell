# 結構註記 — E1-26 後端白名單 lint（相位閘門）

- 層級：engine（治理工具層，非 C++ 單元）／子系統 governance（G1b）／相位 1
- 目錄位置：治理腳本 `scripts/backend_guard.py` + 宣告式資料 `docs/backlog/phase.json`
  - `scripts/backend_guard.py` — G1b 相位閘門。依 phase.json 當前相位取後端白名單，
    強制 `src/kernel/backend/*` 只出現白名單內的後端；並反向掃描 `tests/contract/**/*.py`
    禁止平台分支 token。CLI：`python scripts/backend_guard.py`（`governance.yml` 呼叫）。
  - `docs/backlog/phase.json` — 相位定義 + 各相位後端白名單（`phases` 區塊，相位 1=`null`、
    2=`null,win32`、3=`null,win32,cocoa`）；保留頂層 `phase` / `allowed_backends` 向後相容。
- 測試：`tests/e1/test_backend_guard.py`（Python unittest，14 案例；含真實 repo 樹回歸）。
- 上游相依：E1-22（能力閘控 lint，C++，NFR-02/03 核心）——本單元為其治理腳本層對應，
  分工為「後端目錄相位准入」（E1-26）vs「核心 API 面形狀」（E1-22）。
- 對外介面：`backend_guard.check(root) -> 0|1`；`allowed_backends(phase_dict) -> set`
  （phase.json 相位驅動白名單，無 `phases` 區塊時回退頂層 `allowed_backends`）。
- 相位 1 約束：白名單來源改為宣告式 phase.json，既有 CLI 介面 / 輸出 / exit code 完整保留；
  對當前 repo 樹（僅 `null/` 後端）維持綠燈 exit 0。
