# 結構註記 — E9-04 主題切換

- **目錄位置**：`engine/package/e9_04/`（layer=engine，stage=A）。
- **放了什麼**：
  - `theme.hpp` / `theme.cpp` — 主題管理 API：`ThemeData`（`name` + 宣告式 `attributes:Value` + 可互換 `components:Composition`）、`ThemeResult`、`ThemeChangeCallback`、`theme_from_document()`（E7-07/E7-01 文件 → 主題資料橋接），以及 `ThemeManager` 類別（`register_theme`/`set_theme`/`switch_to`/`current`/`current_name`/`has_current`/`has_theme`/`find_theme`/`list_themes`/`size`/`on_theme_change`）。
  - `CMakeLists.txt` — `add_library(e9_04)`，`target_link_libraries(e9_04 PUBLIC e9_03 e7_07)`；註冊 `test_e9_04`。
- **對外介面（一句話）**：註冊多個視覺主題（顏色/樣式/資源以宣告式資料、元件以 E9-03 組合表示），於執行期切換當前主題並以變更回呼通知相依元件套用；可經 E7-07 熱重載即時套用主題文件；查無主題一律明確報錯、不靜默。
- **相依**：上游 E9-03（`engine/package/e9_03/`，經其再傳遞 E9-01/E9-02）與 E7-07（`engine/format/e7_07/`，經其再傳遞 E7-01/E7-06）。命名空間 `ds::package`，與 E9-01/E9-02/E9-03 一致。
- **平台中立**：純邏輯，無任何 `#ifdef` / 平台分支；API 面不出現絕對座標與數字 z-order（NFR-02）。
- **測試**：`tests/e9/test_e9_04.cpp`（gtest，16 個案例）。
