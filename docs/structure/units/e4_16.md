# 結構註記 — E4-16 輸入建議與補全呈現

- 目錄位置：`modules/elements/e4_16/`（module 層 / 子系統 elements / 平台中立純邏輯）。
- 內容：`suggestion_popup_element.hpp|cpp`（`SuggestionPopupElement` 輸入建議清單彈出
  元件：候選清單 + 選取索引狀態機、以上游 E4-01 `TextLayout` 逐項排版候選文字、綁定
  上游 E4-15 `TextInputElement` 供 `accept()` 套用補全、`render_model()` 產出各候選項
  排版 + 選取標記的相對佈局渲染描述、以（可選）附掛的 E5-13 `KeyboardInputSource`
  接收注入式按鍵事件驅動選取/套用），`CMakeLists.txt`（`add_library(e4_16)` 連結
  `e4_01`/`e4_15` + `test_e4_16`，根層自動 glob 收入）。
- 對外介面：命名空間 `ds::elements`；`SuggestionPopupElement`（set_suggestions/
  suggestions/count/visible、selected_index/selected_text/set_selected_index/
  move_selection、accept、attach/handle_key、set_surface/surface、render_model）；
  `SelectionMove`、`SuggestionItemRenderModel`、`SuggestionPopupRenderModel`。相位 1
  候選清單由呼叫端注入，不做比對/過濾/排序；不含真實鍵盤 / IME。
- 測試：`tests/e4/test_e4_16.cpp`（31 個 gtest 案例，`add_test(NAME e4_16)`）。
