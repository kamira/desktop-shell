# 結構註記 — E4-15 就地輸入框

- 目錄位置：`modules/elements/e4_15/`（module 層 / 子系統 elements / 平台中立純邏輯）。
- 內容：`text_input_element.hpp|cpp`（`TextInputElement` 可編輯文字輸入框：游標/選取狀態機、
  以上游 E4-01 `TextLayout` 排版顯示、以上游 E5-13 `KeyboardInputSource` 接收注入式輸入事件、
  `render_model()` 產出文字+游標+選取的相對佈局渲染描述），`CMakeLists.txt`
  （`add_library(e4_15)` 連結 `e4_01`/`e5_13` + `test_e4_15`，根層自動 glob 收入）。
- 對外介面：命名空間 `ds::elements`；`TextInputElement`（set_text/text、insert/backspace/
  erase_forward、cursor/set_cursor/move_cursor、select/selection/has_selection、
  attach/handle_key/handle_text_input、render_model）。相位 1 不含真實鍵盤 / IME。
- 測試：`tests/e4/test_e4_15.cpp`（34 個 gtest 案例，`add_test(NAME e4_15)`）。
