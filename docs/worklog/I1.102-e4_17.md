# Worklog — I1.102 / E4-17 直方圖

- Unit: E4-17（直方圖）
- Implemented by: I1.102
- Branch: feat/e4_17（從 origin/main 開）
- Risk: low
- Date: 2026-07-24 (UTC+0)
- depends_on: E1-03、E7-03

## 我要做什麼（動工前）

E4-17 是「直方圖」（module 層 / 子系統 elements / phase 1 / low），與最終 CPU/GPU Widget
（歷史用量直方圖）相關：把一序列數值（如歷史 CPU 用量）映射成直方圖 / 時序長條的**渲染描述
模型**——每個 bin / 樣本 → 一根長條，含 y 軸縮放、可選滾動視窗（只留最新 N 筆）、可選閾值線。
相位 1 不做真實繪製，只產出描述供繪製層消費；設定以 E7-03 宣告式段落變數驅動。

計畫產出（全部落在鎖定範圍內）：

1. `modules/elements/e4_17/histogram.hpp|cpp`：`HistogramElement` + `HistogramRenderModel` 及相關型別；
   `configure` / `configure_from_document` 宣告式設定驅動。
2. `modules/elements/e4_17/CMakeLists.txt`：`add_library(e4_17)` PUBLIC 連 `e1_03`、`e7_03`，
   `test_e4_17` + `add_test`。
3. `tests/e4/test_e4_17.cpp`：gtest 覆蓋序列→bins、y 軸縮放、滾動視窗、閾值線、空序列、超範圍夾限、
   E7-03 變數驅動、render_model 內容、非法輸入不靜默。
4. `docs/changes/CHG-20260724-e4_17.md`、本 worklog、`docs/structure/units/e4_17.md`。

## 分層 / NFR 遵循

- module 層純邏輯：只產出渲染描述，不觸碰任何 OS / 繪圖 API。
- NFR-02（無絕對座標 / 無數字 z-order）：長條與閾值皆以正規化幅值 [0,1] 表達，順序以清單 index
  （時間序）表達，非像素座標 / 非數字 z-order。合成不透明度為比例 [0,1]，同樣合規。
- 不靜默失敗：空序列以 `empty=true` 明確回報；超範圍夾限並標記 `clamped`；非有限值 / 無效值域
  回 `Invalid` 且不部分套用。
- 無 `#ifdef _WIN32` / `win32` / `cocoa` 分支，不涉及任何後端。
- 消費上游不重造輪子：合成描述沿用 E1-03 `AlphaProfile`；宣告式設定經 E7-03 `expand()` 展開
  段落變數後再套用。
- 未動根 `CMakeLists.txt`、`units.json`、共用 `docs/structure/directory.md`——G4 以每單元獨立
  結構檔 `docs/structure/units/e4_17.md` 滿足。

## 本機驗證方式

本機無 cmake、無 gtest。正式 gtest/CMake 檔照 BUILD.md 樣板寫好給 CI（ubuntu-latest）。
本地以 `g++ -std=c++17 -Wall -Wextra -Werror` 搭配最小 gtest.h 墊片（scratchpad，不進 repo），
連上游 .cpp（e1_03 + e1_21 + backend_null；e7_03 + e7_02 + e7_01）編譯
`tests/e4/test_e4_17.cpp` + `histogram.cpp`：
→ 19 tests、0 failed、0 failing assertions，零警告，全 PASS。
