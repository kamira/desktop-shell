# Worklog — I1.106 / E4-21 漸層填色

- Unit: E4-21（漸層填色）
- Implemented by: I1.106
- Branch: feat/e4_21（從 origin/main 開）
- Risk: low
- Date: 2026-07-24 (UTC+0)

## 我要做什麼（動工前）

E4-21 是「漸層填色」（module 層 / 子系統 elements / phase 1 / low，depends_on=[E1-03]）：
線性 / 放射狀漸層，多個色停靠點（位置比例 [0,1] → 顏色）+ 角度/方向，產出漸層渲染描述。
相位 1 不做真實繪製。NFR-02：位置用比例、方向用具名/角度，無絕對座標 / 數字 z-order。

計畫產出（全部落在鎖定範圍內）：

1. `modules/elements/e4_21/gradient_fill.hpp`
   - `GradientType{Linear,Radial}`、`Color{r,g,b,a}`（正規化 [0,1]）、`GradientStatus`、
     `GradientStop`、`GradientRenderModel`（含上游 E1-03 `AlphaProfile composite`）。
   - `GradientFill`：`add_stop` / `set_angle` / `set_composite` / `sample(t)` / `render_model()`
     + 唯讀查詢（type / stop_count / stops / angle / composite）。
2. `modules/elements/e4_21/gradient_fill.cpp`：範圍驗證、排序插入、角度正規化、內插取樣、
   render_model 組裝。
3. `modules/elements/e4_21/CMakeLists.txt`：依 BUILD.md 樣板，library（PUBLIC 連 e1_03）+
   `test_e4_21` + `add_test`。
4. `tests/e4/test_e4_21.cpp`：gtest，涵蓋線性/放射、多停靠點排序、sample 內插、角度、
   越界停靠點報錯、無效顏色報錯、render_model。
5. `docs/changes/CHG-20260724-e4_21.md`、本 worklog、`docs/structure/units/e4_21.md`。

## 分層 / 平台中立遵循

- module 層純邏輯：`GradientFill` 只產出渲染描述，不驅動任何真實繪圖 API。
- 無 `#ifdef _WIN32` / `win32` / `cocoa` 分支，不涉及任何後端。
- 未動根 `CMakeLists.txt`、`units.json`、共用 `docs/structure/directory.md`——G4 以每單元獨立
  結構檔 `docs/structure/units/e4_21.md` 滿足。
- 依 BUILD.md 樣板：`target_link_libraries(e4_21 PUBLIC e1_03)`（上游已合併，可讀不可改；本單元
  標頭 include `alpha_surface.hpp` 以沿用 `AlphaProfile` 描述合成，故 PUBLIC 傳遞其 include 目錄）。

## 本機驗證方式

本機無 cmake、無 gtest。正式 gtest/CMake 檔照 BUILD.md 寫好給 CI（ubuntu-latest）。
本地以 `g++ -std=c++17 -Wall -Wextra` 搭配一個最小單次求值 gtest.h 墊片（scratchpad，不進 repo）
編譯 `tests/e4/test_e4_21.cpp` + `gradient_fill.cpp` + 上游 E1-03（`alpha_surface.cpp`）及其相依
（`e1_21/capability_matrix.cpp`、`backend/null/null_backend.cpp`）：
→ 26 tests、87 checks、0 failures，零警告，全 PASS。

## 錯誤紀錄（錯誤 + 根因 + 解法）

- （實作無錯）唯一插曲：`sample()` 用 `std::upper_bound` 找相鄰停靠點時，需同時處理「兩個
  相鄰停靠點 position 相同」的邊界情況（span=0 會除以零）——加了 `span <= 0.0f` 防護、直接
  回下界顏色。屬設計時即發現並處理，非事後除錯。
- 本地墊片編譯沿用先前單元已建立好的 `<gtest/gtest.h>` 墊片（同一 scratchpad，未修改），
  直接 `-I` 指到其父目錄即可，無新插曲。
