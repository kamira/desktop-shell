# Worklog — I1.98 / E1-26 後端白名單 lint（相位閘門）

- Unit: E1-26（engine / phase 1 / risk=low / depends_on E1-22）
- Branch: feat/e1_26 | Implemented by: I1.98 | Skill: ai-sdlc-autopilot

## 做了什麼

把「當前相位允許哪些後端」從 `backend_guard.py` 的硬編碼，改為由
`docs/backlog/phase.json` 的 `phases` 區塊（相位定義 + 各相位後端白名單）驅動。

- `docs/backlog/phase.json`：新增 `phases`（相位 1=`null`、2=`null,win32`、3=`null,win32,cocoa`）；
  保留頂層 `phase` / `allowed_backends` 供向後相容。
- `scripts/backend_guard.py`：拆函式（`load_phase` / `allowed_backends` / `found_backends` /
  `check` / `main`）。`allowed_backends()` 優先取 `phases[當前相位]`，無 `phases` 則回退頂層。
  未定義相位 → 紅燈。契約平台分支反向檢查原封保留。加 `BACKEND_GUARD_ROOT` 供測試覆寫根目錄。
- `tests/e1/test_backend_guard.py`：14 案例 unittest。

## 如何保留 backend_guard 既有行為

- CLI 呼叫 `python scripts/backend_guard.py`（governance.yml G1b）無參數、無環境覆寫 → 行為不變。
- 綠/紅輸出字串與 exit code（0/1）逐字保留。
- 相位 1 允許集在新舊路徑皆為 `{null}`；對當前 repo 樹（僅 `null/`）維持綠燈。
- 測試含「真實 repo 樹回歸」：`check(REPO_ROOT)` 與 `python scripts/backend_guard.py` 子行程皆 exit 0。

## 驗證

- `python3 tests/e1/test_backend_guard.py` → Ran 14 tests, OK。
- `python3 scripts/backend_guard.py` → `backend_guard OK — 相位 1，後端 ['null']`，exit 0。

## 範圍

只寫 write_scope：`scripts/backend_guard.py`、`docs/backlog/phase.json`、
`tests/e1/test_backend_guard.py`、`docs/changes/CHG-20260724-e1_26.md`、
`docs/worklog/I1.98-e1_26.md`、`docs/structure/units/e1_26.md`。未動 directory.md / units.json /
root CMakeLists / 其他腳本 / state.json。
