# Worklog — I1.86 / E7-15 拖放產生設定項

- Date: 2026-07-24 (UTC+0)
- Unit: E7-15 拖放產生設定項（engine 層 / format / Risk: low / 相依 E7-12、E5-08）
- Branch: `feat/e7_15`（從 `origin/main` 開）
- Implementer: I1.86

## 四把鑰匙（scoped ack）

1. 分支：`feat/e7_15`（從 origin/main 開）✓
2. 結構位置：`engine/format/e7_15/**`（承 format 系列於 `engine/format/`，engine 層平台中立）✓
3. 需求切片：拖放產生設定項（E7-15，engine/format / low；相依已合併的 E7-12 設定值寫回、
   E5-08 系統事件）✓
4. 鎖定範圍（唯一可寫）：
   - `engine/format/e7_15/**`
   - `tests/e7/test_e7_15*`
   - `docs/changes/CHG-20260724-e7_15.md`
   - `docs/worklog/I1.86-e7_15.md`
   - `docs/structure/units/e7_15.md`（G4 同步，每單元一檔）
   - **未動** `docs/structure/directory.md`、根 `CMakeLists.txt`、上游、`units.json`

## 我要做什麼（動工前計畫）

消費 E7-12（`set_value` / `serialize`；及其傳遞的 E7-01 `Value` / `Document`）與 E5-08
（`SystemEvent` / `SystemEventSource` / `NullSystemEventSource`），把拖放進來的抽象內容轉成設定項
再寫入宣告式文件。相位 1（Mac / null 期）：純邏輯，**不得**出現 `#ifdef` / win32 / cocoa /
真實後端 / 真實 OS 拖放。

設計（C++17，`namespace ds::format`）：

- 內容：`DropContent{ DropKind kind; std::string payload }`；明確工廠 `file/url/text/color` +
  推斷工廠 `detect`；`detect_drop_kind`（控制位元組 / 空 → Unknown；`#hex` → Color；
  `http/https/ftp://` → Url；絕對路徑 / `file://` / 具副檔名無空白 → File；其餘 → Text）。
- 設定項：`ConfigItem{ path, value }` + `generate_config_item`（圖片→`background.image`、
  一般檔→`file.path`、URL→`link.url`、顏色→`color`、文字→`text`；Unknown → throw）。
- 寫回：`apply_drop(root/doc, drop)` 經 E7-12 `set_value` 純函式重建；`apply_drops` 多筆疊加。
- E5-08 整合：`drop_from_system_event`（以 `detail` 推斷）、`subscribe_drops`（包裝 subscribe，
  事件→DropContent→handler）。相位 1 內容由事件注入；相位 2 後端沿同一分派路徑，語意不動。
- 不靜默失敗：無法辨識內容以 `DropKind::Unknown` 明確回報，產生 / 套用時 throw `std::runtime_error`。

`target_link_libraries(e7_15 PUBLIC e7_12 e5_08)`。無平台分支，相位 2 換後端一行不動。

## 本地驗證

- 本機無 cmake，僅 g++。scratchpad 自製最小 gtest 墊片（單次求值巨集，含 `EXPECT_THROW` /
  `<<` 訊息串 / `main`；不進 repo）。
- 單一命令 `g++ -std=c++17 -Wall -Wextra -Wpedantic` 連 `e7_12` / `e7_01` / `e5_08` 實作編譯
  `tests/e7/test_e7_15.cpp`：**零警告**。
- 執行：**30 tests, 0 failed tests, 0 failed assertions**。
- 正式 gtest / CMake / CTest 照 BUILD.md 由 CI 建置（`add_test(NAME e7_15)`）。

## 結果

- 交付：`dropgen.hpp` / `dropgen.cpp` / `CMakeLists.txt` / `test_e7_15.cpp` /
  `CHG-20260724-e7_15.md` / `I1.86-e7_15.md` / `docs/structure/units/e7_15.md`。
- 未改上游、根 CMakeLists、`directory.md`、`units.json`。相位 1 約束（平台中立、無真實後端）滿足。
