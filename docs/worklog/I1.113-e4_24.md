# Worklog — I1.113 / E4-24 反鋸齒與內距

- Unit: E4-24（反鋸齒與內距）
- Implemented by: I1.113
- Branch: feat/e4_24（從 origin/main 開）
- Risk: low
- Date: 2026-07-24 (UTC+0)
- depends_on: E1-03

## 我要做什麼（動工前）

E4-24 是「反鋸齒與內距」（engine 層 / 子系統 render / phase 1 / low）：渲染層需要一組
共通的反鋸齒（AA 模式、品質）與內容內距（padding / inset）**渲染設定**——純設定邏輯，
不執行任何實際繪製、不綁真實繪圖後端。

先讀 `docs/backlog/BUILD.md`（write_scope / CMakeLists 樣板 / G1-G4 硬性約束），再讀懂
上游 E1-03（`src/kernel/e1_03/alpha_surface.hpp`）的設計精神：具名鍵、正規化比例
（opacity [0,1]、非有限值明確拒絕、有限越界值 clamp）、能力閘控服務層 + 明確狀態回報
不靜默。本單元承此精神——`RenderStyle` 是宣告式輸入，`RenderStyleService::apply()` 驗證
+ 正規化後才落地為 `RenderModel`，失敗不留半份狀態（原子性）。同儕單元 `engine/render/e4_22/`
（2D 變形矩陣）示範了同一 depends_on=E1-03 的連結樣式（僅 CMake `PUBLIC e1_03` 連結表達
「宣告於 surface 模型之上」的關係，不必然需要 include 上游標頭），本單元採同一樣式。

計畫產出（全部落在鎖定範圍內）：

1. `engine/render/e4_24/render_style.hpp` — `AntiAliasMode` / `RenderQuality` /
   `SpacingToken` / `InsetUnit` / `InsetValue` / `Insets` / `RenderStyle` /
   `RenderConfigStatus` / `RenderModel` / `resolve_spacing_token()` / `RenderStyleService`。
2. `engine/render/e4_24/render_style.cpp` — 列舉合法性檢查、內距單側解析（具名查表 /
   比例有限性 + 夾限）、`RenderStyleService::apply()` / `render_model()`。
3. `engine/render/e4_24/CMakeLists.txt` — 依 BUILD.md 樣板，library + `PUBLIC e1_03` +
   `test_e4_24` + `add_test`。
4. `tests/e4/test_e4_24.cpp` — gtest，涵蓋 AA 模式、品質、內距設定（比例 / 具名）、
   內距夾限、無效輸入報錯（未知列舉值 / 非有限值）、`render_model()` 查詢語意。
5. `docs/changes/CHG-20260724-e4_24.md`、本 worklog、`docs/structure/units/e4_24.md`。

## 分層 / 平台中立遵循

- engine 層純設定邏輯：無真實繪圖後端、無 OS 呼叫，不執行任何實際繪製。
- 無 `#ifdef _WIN32` / `win32` / `cocoa` 分支；phase 1 平台中立。
- 內距一律以正規化比例 [0,1] 或具名間距權杖表達，不含硬編絕對座標 / 數字 z-order
  （NFR-02）；未知列舉值（含 `static_cast` 硬轉出範圍）與非有限值（NaN/Inf）皆以
  `RenderConfigStatus::Invalid` 明確回報，不靜默；有限但越界的比例則明確**夾限**
  （非錯誤，與 E1-03 opacity 同精神）。
- 未動根 `CMakeLists.txt`、`units.json`、共用 `docs/structure/directory.md`、上游
  e1_03——G4 以每單元獨立結構檔 `docs/structure/units/e4_24.md` 滿足。

## 本機驗證方式

本機無 cmake、無 gtest。正式 gtest/CMake 檔照 BUILD.md 寫好給 CI。
本地以 scratchpad 既有的最小 gtest 墊片（`gtest/gtest.h`，巨集單次求值，不進 repo）搭配
`g++ -std=c++17 -Wall -Wextra`，單一指令、全絕對路徑、無 cd / 無重導，直接編譯
`tests/e4/test_e4_24.cpp` + `render_style.cpp`，並**連上游 e1_03 鏈**
（`alpha_surface.cpp` + `capability_matrix.cpp`（E1-21）+ `null_backend.cpp`（backend_null）
一併納入編譯，驗證 CMake `PUBLIC e1_03` 連結成立、無符號衝突）：

→ 31 tests、0 failed、零警告，全 PASS。

## 錯誤紀錄（錯誤 + 根因 + 解法）

無。設計階段即比照 E1-03 / E4-22 既有模式（具名列舉、正規化比例、明確狀態回報、
窮舉 switch 不寫 default 讓越界列舉值明確落到「不合法」分支），一次寫成即通過本機驗證，
未遇到需要返工的編譯或邏輯錯誤。
