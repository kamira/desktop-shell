# Worklog — I1.22 / E4-09 動畫驅動源

- Date: 2026-07-24 (UTC+0)
- Unit: E4-09 動畫驅動源（engine 層 / render 繪製基座 / Risk: low）
- Branch: `feat/e4_09`（從 `origin/main` 開）
- Implementer: I1.22
- 相依上游：E5-04 心跳事件（已合併）

## 四把鑰匙（scoped ack）

1. 分支：`feat/e4_09`（從 main 開）✓
2. 結構位置：`engine/render/e4_09/**` ✓（engine 層純邏輯，依 BUILD.md glob `engine/*/*/`）
3. 需求切片：動畫驅動源（E4-09，engine / render / low）——消費 E5-04 心跳，餵 dt 給動畫回呼 ✓
4. 鎖定範圍（唯一可寫）：
   - `engine/render/e4_09/**`
   - `tests/e4/test_e4_09*`
   - `docs/changes/CHG-20260724-e4_09.md`
   - `docs/worklog/I1.22-e4_09.md`
   - `docs/structure/units/e4_09.md`（G4；每單元獨立結構檔，不動共用 `directory.md`）

## 我要做什麼（動工前計畫）

engine 層純邏輯，平台無關，消費上游 E5-04 心跳：

- `AnimationDriver`（`animation_driver.hpp|cpp`）：建構時綁一個 `HeartbeatSource&`，以
  `pulse_interval`（tick）訂閱之。每次心跳脈衝 → 對每個「未暫停」動畫餵一份 dt（= 心跳間隔），
  並累計該動畫的 elapsed 與 frame，透過 `AnimationCallback(const AnimationFrame&)` 通知消費者。
- 動畫管理：`add`（配發遞增 `AnimationId`，空回呼回 0）/ `remove` / `pause` / `resume` /
  `is_paused`；另有整體 `pause_all` / `resume_all` / `paused`。
- 派發兩階段（承 E5-04）：先結算狀態收集待呼叫幀、再統一觸發，回呼中變更動畫集不破壞本次派發。
- 生命週期：解構自動解訂閱；訂閱閉包持 `this`，故刪除複製 / 移動。
- 不接真實時鐘 / 計時器 / 繪製後端、無平台分支（無 `#ifdef` / win32 / cocoa）。確定性 → 可完全單元測試。

## 本地驗證

本機無 cmake，只有 g++。以 scratchpad 內自製最小 gtest 墊片（不進 repo，另建獨立副本避免與
其他並行單元的共用墊片衝突，並補上 `SUCCEED()`）`g++ -std=c++17 -Wall -Wextra` 連 E5-04
未改動的實作，編譯未經改動的 `tests/e4/test_e4_09.cpp` 驗證邏輯；正式 gtest/CMake 照 BUILD.md 寫給 CI。

## 本地驗證結果

```
g++ -std=c++17 -Wall -Wextra -I <shim> -I engine/render/e4_09 -I engine/events/e5_04 \
    engine/render/e4_09/animation_driver.cpp engine/events/e5_04/heartbeat_source.cpp \
    tests/e4/test_e4_09.cpp -o test_e4_09_local
→ BUILD OK（零警告）
./test_e4_09_local
→ 12 tests run, 0 failing assertions（全 PASS）
```

12 個測試：心跳推進動畫、幀資料累計、單次 advance 跨多脈衝、暫停/繼續、移除、多動畫獨立、
整體暫停、pulse_interval 夾限、空回呼拒絕、未知 id 安全、解構解訂閱、回呼中變更動畫集安全。

## 遇到的錯誤

- 實作錯誤：無。實作與測試編譯一次過（`-Wall -Wextra` 零警告），12 個測試全 PASS。
- 流程摩擦（非程式錯誤，記錄備查）：
  1. 首次寫測試檔誤打一個佔位識別碼（含非 ASCII 字元）→ 立即整檔重寫更正，未影響後續。
  2. scratchpad 既有共用 gtest 墊片缺 `SUCCEED()` 巨集 → 另建本單元專屬墊片副本並補上，
     不覆蓋他人共用檔。

## 不含（避免越界）

- 不接真實時鐘 / 計時器；不與繪製後端接線；僅把心跳脈衝轉為「時間 → 動畫推進」。
- 不改上游 E5-04（僅 `PUBLIC` 連結使用）。
- 未動 `units.json`、根 `CMakeLists.txt`、`docs/structure/directory.md` 或任何鎖定範圍以外的檔案。
