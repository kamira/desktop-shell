# Worklog — I1.64 / E7-09 缺席模組的降級解析

- Date: 2026-07-24 (UTC+0)
- Unit: E7-09 缺席模組的降級解析（engine 層 / 描述子系統 / stage A / Risk: low / depends_on [E7-01, E9-02]）
- Branch: `feat/e7_09`（從 `origin/main` 開）
- Implementer: I1.64

## 四把鑰匙（scoped ack）

1. 分支：`feat/e7_09`（從 origin/main 開）✓
2. 結構位置：`engine/format/e7_09/**`（engine 層平台中立純邏輯；消費 E7-01 `Value` + E9-02 manifest）✓
3. 需求切片：缺席模組的降級解析（E7-09，engine / 描述 / low / depends_on [E7-01, E9-02]）✓
4. 鎖定範圍（唯一可寫）：
   - `engine/format/e7_09/**`
   - `tests/e7/test_e7_09.cpp`
   - `docs/changes/CHG-20260724-e7_09.md`
   - `docs/worklog/I1.64-e7_09.md`
   - `docs/structure/units/e7_09.md`（G4；**不動** `directory.md`）

## 我要做什麼（動工前計畫）

設定文件引用了**當前不存在 / 未安裝的模組**時，不硬失敗，而是**優雅降級解析**：偵測缺席模組、
以停用佔位取代該區塊、產出可用的降級後 `Value` + 一份可見的降級決策清單。

設計（C++17，`namespace ds::format`）：

- 模組引用慣例：Map 含字串鍵 `module`（`kModuleKey`）即該區塊「由該 module id 擁有」。
- `ModuleAvailability::has(id)`（可注入介面）；來源 `ModuleSet` / `FnAvailability` /
  `available_from_manifests`（由 E9-02 `Manifest.name` 清單建立）。
- `resolve_with_fallback(Value, const ModuleAvailability&) -> DegradeResult{value, missing[], notes[]}`：
  可用區塊原樣保留並續遞迴巢狀；缺席區塊換停用佔位（保留 id + `disabled` + `degraded_reason`），
  仍掃描其子樹回報巢狀缺席；決策可見不靜默；`missing` 去重、`notes` 逐次帶 path。

讀懂上游（可讀不可改）：`engine/format/e7_01/document.hpp`（`Value` 工廠 / 型別查詢 / `find`/`at`、
深層 `operator==`、`parse()`）與 `engine/package/e9_02/manifest.hpp`（`Manifest.name`）。

## 本地驗證

- 本機無 cmake，僅 g++。scratchpad 最小 gtest 墊片（`TEST` 註冊 + `EXPECT_*/ASSERT_*` 單次求值 + `main`）。
- 單一命令，全絕對路徑，無 cd / 無重導：
  `g++ -std=c++17 -Wall -Wextra -I<shim> -I<e7_09> -I<e7_01> -I<e9_02> tests/e7/test_e7_09.cpp
  engine/format/e7_09/fallback.cpp engine/format/e7_01/document.cpp engine/package/e9_02/manifest.cpp -o <bin>`
- 結果：**零警告，11 tests / 0 failing**。
- 正式 gtest / CMake 依 BUILD.md 由 CI 跑（`add_test(NAME e7_09)`）。

## 收尾

- commit in-scope 五類檔案於 `feat/e7_09`，訊息 `feat(format): 缺席模組的降級解析 [CHG-20260724-e7_09]`。
- 不 push、不開 PR。Risk low → full auto。
