# 工作日誌 — I1.97 / E1-02 輸入策略四態

- 單元：E1-02（輸入策略四態，platform / kernel / phase 1 / null，risk=medium）
- 分支：feat/e1_02（自 origin/main）
- depends_on：E1-25（契約）；建於 E1-24（K3 輸入原語）之上
- Skill：ai-sdlc-autopilot

## 先讀

- `docs/backlog/BUILD.md`：CMake 慣例、write_scope、G1/G1b/G3/G4 閘門、相位 1 = null 約束。
- E1-24 `src/kernel/backend/null/null_backend.hpp` / `.cpp`：`KernelBackend` 介面、K3 輸入原語
  （`set_input_policy` / `poll_input`）、`InputPolicy`（**三態**）、`InputEvent`、`SurfaceId`。
- E1-25 `tests/contract/kernel_backend.hpp`：契約。
- E1-21 `capability_matrix.hpp` / `.cpp`：`CapabilityMatrix::defaults()`、`has()` 語意。

## 關鍵發現

E1-24 的 `InputPolicy` **只定義三態**（`Modal` / `Accepting` / `PassThrough`），非四態。
依單元規格「若上游已定義四態則沿用，否則對齊」，本單元新增獨立四態列舉 `InputStrategy`
（`Interactive` / `Capture` / `ClickThrough` / `Inert`），並以 `to_backend_policy()` 對齊下推
回 E1-24 三態。第四態 `Inert`（實心命中但吞掉、不下傳）無 E1-24 對應，取命中相符的 `Accepting`
下推、吞掉語意由 `InputStrategyController` 路由層補足（唯一有損對映，已具名記錄於 CHG）。

## 產出

- `src/kernel/e1_02/input_strategy.hpp` / `.cpp` / `CMakeLists.txt`
- `tests/e1/test_e1_02.cpp`（23 gtest 案例）
- `docs/changes/CHG-20260724-e1_02.md`、`docs/structure/units/e1_02.md`、本日誌

## 本機驗證

單一 `g++ -std=c++17 -Wall -Wextra` 連上游 `capability_matrix.cpp` + `null_backend.cpp` +
`input_strategy.cpp` + `test_e1_02.cpp` + scratchpad gtest 墊片（單次求值、未進 repo）：
**零警告、23/23 全過**。涵蓋四態純函式（命中 / 路由 / 對齊 E1-24 / 具名字串）、預設策略、
set/query、狀態轉換、has() 能力閘控（NFR-03）、跨 surface 路由（穿透 / 吞掉 / 全域獨占）、
K3 poll_input 整合、基底多型。

## 約束遵循

- 只寫 write_scope（`src/kernel/e1_02/**`、`tests/e1/test_e1_02.cpp`、CHG / 結構 / 日誌）。
- 未動根 CMakeLists / 上游 / units.json；未動共用 `directory.md`（每單元獨立結構檔，G4）。
- 無 `#ifdef` / win32 / cocoa；NFR-02 具名（四態列舉、無座標 / 數字 z-order）；NFR-03 has() 閘控。
