# Worklog — I1.122 / E12-02 多語系

- Date: 2026-07-24 (UTC+0)
- Unit: E12-02 多語系（engine 層 / 子系統 common(?) / 相依 E4-01 / phase 1 / Risk: low）
- Branch: `feat/e12_02`（從 `origin/main` 開）
- Implementer: I1.122

## 四把鑰匙（scoped ack）

1. 分支：`feat/e12_02`（從 `origin/main` 開）✓
2. 結構位置：`engine/common/e12_02/**`（engine 層純邏輯，依 BUILD.md glob `engine/*/*/`）✓
3. 需求切片：多語系 / i18n（E12-02，low，命名鍵→翻譯字串查詢、語系切換、缺翻譯回退、
   佔位替換、可選複數形式），命名空間 `ds::common`✓
4. 鎖定範圍（唯一可寫）：
   - `engine/common/e12_02/**`
   - `tests/e12/test_e12_02.cpp`
   - `docs/changes/CHG-20260724-e12_02.md`
   - `docs/worklog/I1.122-e12_02.md`
   - `docs/structure/units/e12_02.md`（G4；每單元獨立結構檔，不動共用 `directory.md`）

## 先讀

- `docs/backlog/BUILD.md`（建置慣例）。
- 上游 `engine/render/e4_01/`（文字排版；已合併，可讀不可改）：確認本單元產出的
  在地化文字即其 `layout()` 的輸入字串，本單元標頭不需 include 其標頭。
- `engine/common/e12_01/`（idle 門檻；engine/common 慣例參考）：`ds::common` 命名空間、
  鏈式 setter、`to_string` 診斷、契約測試風格皆延續此例。

## 我要做什麼（動工前計畫）

建立一個多語系翻譯目錄 `LocaleCatalog`：具名訊息鍵 → 各語系翻譯字串的查詢、當前語系
切換、缺翻譯明確可見回退（退預設語系 / 退鍵本身，絕不靜默崩潰）、參數化訊息（`{name}`
佔位替換）、可選複數形式（單 / 複數二態）。平台中立純邏輯，翻譯資料由呼叫端注入。

## 我做了什麼

- `engine/common/e12_02/locale_catalog.hpp` / `.cpp`（`namespace ds::common`）：
  - `LocaleId` / `MessageKey` / `TranslationArgs` 型別別名。
  - `TranslationStatus`（Exact / LocaleFallback / KeyFallback）+ `to_string`。
  - `TranslationResult{text, status, locale_used}`。
  - `LocaleCatalog`：`add_translation` / `add_plural`（鏈式）、`set_locale` /
    `current_locale()` / `default_locale()`、`has_key`（二重載）、`translate` /
    `translate_detailed` / `translate_plural`。
  - `substitute_placeholders(text, args)`：`{name}` 佔位替換自由函式，對外公開。
- `engine/common/e12_02/CMakeLists.txt`：`add_library(e12_02)` + `PUBLIC e4_01` +
  `test_e12_02`（`tests/e12/test_e12_02.cpp`）+ `add_test(NAME e12_02)`。
- `tests/e12/test_e12_02.cpp`：gtest 31 案例（建構契約 / 註冊查詢 / 語系切換 / 多語系並存 /
  缺翻譯回退（兩層）/ 缺鍵處理 / 佔位替換 / 複數形式 / 診斷字串）。
- 文件：CHG-20260724-e12_02、本 worklog、`docs/structure/units/e12_02.md`（G4）。

## 本機驗證

`g++ -std=c++17 -Wall -Wextra -Wpedantic` 編譯 `locale_catalog.cpp` + `test_e12_02.cpp` +
scratchpad 自製 gtest 巨集墊片（單次求值，不進 repo；提供 `TEST` / `EXPECT_EQ` /
`EXPECT_TRUE` / `EXPECT_FALSE` / `EXPECT_STREQ` / `EXPECT_THROW` / `EXPECT_NO_THROW` 子集）：
**零警告**。執行：**31 test(s) run, 0 failure(s)**。

CMake 宣告相依 E4-01（`PUBLIC e4_01`），但本單元標頭 / 實作皆未 include E4-01 標頭
（在地化文字是 E4-01 排版的輸入，關係在呼叫端串接，非標頭層相依），故本機語法驗證
未額外連結 E4-01 / E1-03 上游 .cpp（不影響本單元程式碼本身的正確性驗證）。

## 硬性約束自檢

- 只寫 write_scope 內路徑；未動 root CMakeLists / 上游（E4-01）/ units.json / `directory.md`。✓
- 平台中立：無 `#ifdef` / 無平台分支 / 無真實 locale·ICU·OS API（翻譯資料由呼叫端注入）。✓
- 有可跑 CTest 測試（`add_test(NAME e12_02)`），31 案例非零。✓
- 核心 API 無絕對座標 / 無數字 z-order（NFR-02 不適用本純邏輯單元）。✓
- 缺鍵 / 缺語系明確可見回退，不靜默、不崩潰（`translate()` 系列不擲例外；`KeyFallback`
  以 `[missing:key]` 可見標記呈現）。✓

## 問題 / 備註

- 無阻塞問題。複數形式簡化為單 / 複數二態（非完整 CLDR 多複數規則），相位 1 契約單元
  之最小可用實作；規格標註複數形式為「可選」，未來相位可視語系需求擴充規則引擎。
- E12-02 尚無現存消費者；未來 E4-01 文字元件可在 `layout()` 前先以 `translate()` 解析
  具名鍵為顯示字串。
