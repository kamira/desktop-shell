# Worklog — I1.87 / E12-01 idle 記憶體與 CPU 門檻

- Date: 2026-07-24 (UTC+0)
- Unit: E12-01 idle 記憶體與 CPU 門檻（engine 層 / 子系統 common / 相依 E2-02 / 對應 NFR-01 / Risk: low）
- Branch: `feat/e12_01`（從 `origin/main` 開）
- Implementer: I1.87

## 四把鑰匙（scoped ack）

1. 分支：`feat/e12_01`（`git fetch origin main` 後從 `origin/main` 開）✓
2. 結構位置：`engine/common/e12_01/**`（engine 層純邏輯，依 BUILD.md glob `engine/*/*/`）✓
3. 需求切片：idle 記憶體與 CPU 門檻（E12-01，low，對應 NFR-01「閒置時低資源占用」）✓
4. 鎖定範圍（唯一可寫）：
   - `engine/common/e12_01/**`
   - `tests/e12/test_e12_01.cpp`
   - `docs/changes/CHG-20260724-e12_01.md`
   - `docs/worklog/I1.87-e12_01.md`
   - `docs/structure/units/e12_01.md`（G4；每單元獨立結構檔，不動共用 `directory.md`）

## 我要做什麼（動工前計畫）

在 E2-02 採集分級之上，建立一個**閒置資源門檻管理器**：依注入的 CPU / 記憶體用量判定
系統閒置或活躍，據此建議採集活動等級，並與 E2-02 的 `SamplingTier` 整合（閒置時把分級降頻）。
關鍵：用**雙門檻遲滯**避免臨界抖動。平台中立純邏輯，讀值由呼叫端注入。

## 我做了什麼

- `engine/common/e12_01/idle_threshold.hpp` / `.cpp`（`namespace ds::common`）：
  - `ActivityLevel`（Active / Idle）+ `to_string` + `lower_tier`（降 SamplingTier 一階）。
  - `IdleThresholdPolicy`：`set_thresholds(cpu%, mem%)`（夾 [0,100]）、`set_hysteresis`
    （夾 >=0）、`set_tier_mapping`；`evaluate(cpu%, mem%)->ActivityLevel`（OR 進活躍 / AND 出
    閒置 / 死區維持現態的雙門檻遲滯）；`recommended_tier()`、`adjust_tier(base)`。
- `engine/common/e12_01/CMakeLists.txt`：`add_library(e12_01)` + `PUBLIC e2_02` +
  `test_e12_01`（`tests/e12/test_e12_01.cpp`）+ `add_test(NAME e12_01)`。
- `tests/e12/test_e12_01.cpp`：gtest 22 案例（低於門檻降頻 / 超門檻升頻 / 遲滯防抖 /
  E2-02 tier 對應 / 邊界值 / 門檻設定驗證 / 端到端轉換）。
- 文件：CHG-20260724-e12_01、本 worklog、`docs/structure/units/e12_01.md`（G4）。

## 本機驗證

`g++ -std=c++17 -Wall -Wextra` 連上游 `e2_02/sampling.cpp` + `e2_01/metric.cpp` + scratchpad
gtest 墊片（單次求值，不進 repo）編譯：**零警告**。執行：**22 passed, 0 failed, 22 total**。

## 硬性約束自檢

- 只寫 write_scope 內路徑；未動 root CMakeLists / 上游 / units.json / `directory.md`。✓
- 平台中立：無 `#ifdef` / 無平台分支 / 無真實資源 API（讀值由呼叫端注入）。✓
- 有可跑 CTest 測試（`add_test(NAME e12_01)`），非零案例。✓
- 核心 API 無絕對座標 / 無數字 z-order（NFR-02 不適用本純邏輯單元）。✓

## 問題 / 備註

- 無阻塞問題。E12-01 為契約 / 純邏輯單元，尚無現存消費者；未來採集迴圈注入真實 sysinfo
  讀值後即可驅動 E2-02 排程分級（閒置降頻）。
