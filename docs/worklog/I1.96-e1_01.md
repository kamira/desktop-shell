# Worklog — I1.96 / E1-01 具名圖層列舉與 z-order 維持

- Unit: E1-01（具名圖層列舉與 z-order 維持）
- Implemented by: I1.96
- Branch: feat/e1_01（從 origin/main 開）
- Risk: medium（高影響——堆疊順序是所有視覺元件共享的排序契約）
- Date: 2026-07-24 (UTC+0)

## 我要做什麼（動工前）

E1-01 是「具名圖層列舉與 z-order 維持」（platform 層 / 子系統 kernel / 相位 1 null）。桌面
元件分屬具名圖層（桌布 / 一般視窗之下 / 一般 / 浮層 / 最上層），堆疊順序由**圖層語意**決定，
**不得用數字 z-index**（NFR-02 鐵律）。建於上游 E1-24 的 `SurfaceLayer` 具名圖層詞彙之上，
提供：列舉具名圖層、把 surface 指派到圖層、查詢 / 維持堆疊順序、圖層間相對順序規則。

計畫產出（全部落在鎖定 write_scope 內）：

1. `src/kernel/e1_01/layer_stack.hpp` / `.cpp`：`layers_bottom_to_top` / `layer_name` /
   `compare_layers` / `LayerRelation` / `LayerAssign` / `LayerStack`。
2. `src/kernel/e1_01/CMakeLists.txt`：`e1_01` library + `test_e1_01` + `add_test`，
   `target_link_libraries(e1_01 PUBLIC e1_21 backend_null)`。
3. `tests/e1/test_e1_01.cpp`：gtest 涵蓋列舉、相對順序、指派、跨層 / 同層堆疊、語意優先於
   加入序、has() 閘控、未指派保守。
4. `docs/changes/CHG-20260724-e1_01.md`、本 worklog、`docs/structure/units/e1_01.md`。

## NFR-02 / NFR-03 / 分層遵循

- **NFR-02（鐵律）**：對外 API 只有具名圖層（`SurfaceLayer`）與具名關係（`LayerRelation`）；
  無數字 z-order / z-index、無絕對座標。內部語意序 `semantic_index` 為 private，不對外當數字
  層級用；堆疊順序不以整數欄位儲存，而是每次由「由底到頂具名圖層清單 × 加入序」重新導出，
  確保**圖層語意優先於加入序**。原始碼與測試已對照 E1-22 lint 規則（`z[_-]?order|z[_-]?index
  = 數字`、座標欄位賦值）自查，零命中。
- **NFR-03**：改動堆疊狀態的 `assign` / `remove` 一律先 `has("kernel.surface")` 閘控；能力
  不可用 → 結構化拒絕、不改狀態、不崩潰。未知能力沿用上游 E1-21 保守回 false。
- **平台中立 / 相位 1**：無 `#ifdef` / `win32` / `cocoa` / 真實 OS API；未建 `backend/win32|cocoa/`。
- 未動根 `CMakeLists.txt`、`units.json`、共用 `docs/structure/directory.md`——G4 以每單元獨立
  結構檔 `docs/structure/units/e1_01.md` 滿足。

## 本機驗證方式

本機無 cmake / gtest。正式 gtest / CMake 檔照 BUILD.md 樣板寫好給 CI（FetchContent googletest）。
本地以 `g++ -std=c++17 -Wall -Wextra` 連上游 `e1_21/capability_matrix.cpp` +
`backend/null/null_backend.cpp` + 本單元 `layer_stack.cpp` + `tests/e1/test_e1_01.cpp`，
搭配一個最小單次求值 gtest.h 墊片（scratchpad，不進 repo）編譯執行：
→ **17 tests、全 PASS、0 failing assertions、零警告（-Wall -Wextra 全過）**。

## 結果

- 17 個 gtest 案例全綠；NFR-02 / NFR-03 自查通過；純新增單元，無上游 / 根建置 / 共用檔改動。
